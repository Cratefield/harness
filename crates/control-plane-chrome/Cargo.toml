[package]
name = "cratefield-chrome"
description = "The control plane's own page chrome: cratefield.com's tokens and component vocabulary, served once and shared by every control-plane screen"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
cratefield-core = { workspace = true }
axum = { workspace = true }
http = { workspace = true }

[dev-dependencies]
cratefield-testing = { path = "../testing" }
pollster = { workspace = true }
tower = { workspace = true }
